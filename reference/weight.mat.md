# Convert dataframe to matrix

Convert dataframe to matrix

## Usage

``` r
weight.mat(data)
```

## Arguments

- data:

  dataframe of weight

## Value

A matrix

## Examples

``` r
weight.mat(data = weight)
#> Error in weight.mat(data = weight): could not find function "weight.mat"
```
