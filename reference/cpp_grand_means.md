# Compute Grand Means

Computes mean for each trait (column).

## Usage

``` r
cpp_grand_means(data_mat)
```

## Arguments

- data_mat:

  Numeric matrix (n_obs x n_traits)

## Value

Vector of grand means (n_traits)
