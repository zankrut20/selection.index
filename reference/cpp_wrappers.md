# C++ Function Wrappers with Uniform Validation

Thin R wrapper functions that call cpp\_\* functions with uniform input
validation, type checking, and error handling. Centralizes validation
logic and provides consistent error messages across the package.
