# Tolerance for zero checks (near-zero values) Values smaller than this in absolute value are considered zero

Tolerance for zero checks (near-zero values) Values smaller than this in
absolute value are considered zero

## Usage

``` r
TOL_ZERO
```

## Format

An object of class `numeric` of length 1.
