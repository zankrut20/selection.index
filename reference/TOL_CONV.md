# Tolerance for convergence in iterative algorithms Default convergence criterion for missing value imputation

Tolerance for convergence in iterative algorithms Default convergence
criterion for missing value imputation

## Usage

``` r
TOL_CONV
```

## Format

An object of class `numeric` of length 1.
