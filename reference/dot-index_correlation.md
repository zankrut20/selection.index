# Compute correlation between indices at two stages

Compute correlation between indices at two stages

## Usage

``` r
.index_correlation(b1, b2, P1, P, stage1_indices = NULL)
```

## Arguments

- stage1_indices:

  Indices of stage 1 traits in the full matrix (default: 1:nrow(P1))
