# Compute Trait-wise Min/Max

Computes minimum and maximum for each trait.

## Usage

``` r
cpp_trait_minmax(data_mat)
```

## Arguments

- data_mat:

  Numeric matrix (n_obs x n_traits)

## Value

List with 'min' and 'max' vectors
