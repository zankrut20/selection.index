# Tolerance for symmetry checks (e.g., isSymmetric) Used when checking if matrices are symmetric within numerical precision

Tolerance for symmetry checks (e.g., isSymmetric) Used when checking if
matrices are symmetric within numerical precision

## Usage

``` r
TOL_SYM
```

## Format

An object of class `numeric` of length 1.
